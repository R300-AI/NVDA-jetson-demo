# 透過C++實作單核向量加法

### 題目說明
 1. 利用 `std::vector<float>`建立大小為[1, 10^8]的隨機向量𝐴, 𝐵 
 2. 使用 for-loop完成𝐴 + 𝐵中每個item的計算
 3. 利用 `std::chrono` 記錄整體執行時間

### 作業練習
觀察 CPU 中單一核心的最高的核心使用率與頻率，並監測 執行期間的CPU功耗輸出峰值
在編譯指令中加入-O2啟動向量化(NEON /SIMD) ，比較邏輯加速前後的效能差異

