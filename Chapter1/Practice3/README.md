# 透過OpenBLAS實踐多核資源調度

### 題目說明
1. 使用與**例題1-1**相同的[1, 10^8]向量 𝐴, 𝐵 
2. 呼叫 `openblas_set_num_threads(n)` 調整執行緒數量（例如 n = 1, 2, 4, 8）
3.使用 `cblas_saxpy()` 完成C = A + B運算
4. 利用` std::chrono` 記錄整體執行時間

### 作業練習
觀察並比較不同執行緒數量下的執行時間變化，分析多核資源調度的非線性效能提升，並監測 VDD_CPU 功耗輸出情況
