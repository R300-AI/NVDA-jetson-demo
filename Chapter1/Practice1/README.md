# 透過C++實作一個簡單的向量加法

### 題目說明
 1. 利用 `std::vector<float>`及 `std::generate`建立大小為 [1, 10^8] 的隨機向量𝐴, 𝐵 
 2. 使用 for-loop計算𝐴 + 𝐵的執行結果
 3. 利用 `std::chrono` 記錄整體執行時間

### 作業練習
觀察到單一核心在執行期間時間的最高頻率，並監測 VDD_CPU 功耗的輸出情況
